const defaultState = {
    currentUser: {},
    isLogin: false
}


export default function userReducer(state = defaultState, action) {
    switch (action.type) {
        default:
            return state
    }
}
